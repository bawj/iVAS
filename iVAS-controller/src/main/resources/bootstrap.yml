spring:
  profiles:
    active: dev

mybatis-plus:
  global-config:
    banner: false
#    关闭sql打印日志
#  configuration:
#    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

ribbon:
  eager-load:
    enabled: true
    clients: ynivpas-manager
